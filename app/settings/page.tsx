import { Placeholder } from "@/components/Placeholder";

export const metadata = { title: "Ajustes" };

export default function SettingsPage() {
  return (
    <Placeholder title="Ajustes" glyph="設" phase="Fase 7 · Pulido">
      Tarjetas nuevas por día, tema, autoplay de audio, reducir movimiento y enlaces a los datos y
      atribuciones.
    </Placeholder>
  );
}

import { Placeholder } from "@/components/Placeholder";

export const metadata = { title: "Kana" };

export default function KanaPage() {
  return (
    <Placeholder title="Entrenador de kana" glyph="あ" phase="Fase 3 · Kana">
      El drill de hiragana y katakana (reconocimiento + recall), con la tabla gojūon como artefacto
      y sus propias tarjetas FSRS.
    </Placeholder>
  );
}

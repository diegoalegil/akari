# Cloudflare's build runs with NODE_ENV=production, which makes npm skip
# devDependencies — but the build needs them (tsx for the seed, tailwind +
# typescript for `next build`). Force them to install.
include=dev
